"""Physical module packages for Ojo de Dios.

Each child package corresponds to one product module catalog slot. The package
and its manifest describe structure only; execution readiness is still resolved
through catalog metadata, ToolHealth, VersionLock, workers, permissions, scope,
and evidence state.
"""
